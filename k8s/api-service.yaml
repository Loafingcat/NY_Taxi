apiVersion: v1
kind: Service
metadata:
  name: ny-taxi-api-service
spec:
  type: NodePort
  selector:
    app: ny-taxi-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080