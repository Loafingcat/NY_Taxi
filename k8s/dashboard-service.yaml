apiVersion: v1
kind: Service
metadata:
  name: ny-taxi-dashboard-service
spec:
  type: NodePort
  selector:
    app: ny-taxi-dashboard
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30085